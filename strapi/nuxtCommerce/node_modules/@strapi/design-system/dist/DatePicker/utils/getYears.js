const c = (r, e) => {
  var E, n;
  const t = new Date().getFullYear(), o = (E = r == null ? void 0 : r.getFullYear()) != null ? E : parseInt(t, 10) - 200, F = (n = e == null ? void 0 : e.getFullYear()) != null ? n : parseInt(t, 10) + 15, A = [];
  for (let s = o; s <= F; s++)
    A.push(s);
  return A;
};
export {
  c as getYears
};
