import s from "styled-components";
import { Stack as r } from "../Stack/Stack.js";
const e = s(r).attrs({
  horizontal: !0,
  spacing: 2
})`
  position: absolute;
  top: ${({ theme: t }) => t.spaces[3]};
  right: ${({ position: t, theme: o }) => t === "end" ? o.spaces[3] : void 0};
  left: ${({ position: t, theme: o }) => t === "start" ? o.spaces[3] : void 0};
`;
export {
  e as CardAction
};
