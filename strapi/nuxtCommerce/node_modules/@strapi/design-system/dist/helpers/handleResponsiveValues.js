const a = (o, s, i) => {
  let r = s;
  if (!Array.isArray(s) && typeof s == "object" && (r = [s == null ? void 0 : s.desktop, s == null ? void 0 : s.tablet, s == null ? void 0 : s.mobile]), r === void 0)
    return;
  if (Array.isArray(r)) {
    const [n, d, $] = r;
    let t = `${o}: ${i.spaces[n]};`;
    return d !== void 0 && (t += `${i.mediaQueries.tablet}{
          ${o}: ${i.spaces[d]};
        }`), $ !== void 0 && (t += `${i.mediaQueries.mobile}{
          ${o}: ${i.spaces[$]};
        }`), t;
  }
  const c = i.spaces[r] || r;
  return `${o}: ${c};`;
};
export {
  a as default
};
