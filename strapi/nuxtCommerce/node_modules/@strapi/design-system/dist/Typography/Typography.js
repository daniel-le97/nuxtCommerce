import r from "styled-components";
import { variantStyle as i, ellipsisStyle as a, handleColor as n } from "./utils.js";
import { typographyDefaultProps as p, typographyPropTypes as s } from "./TypographyProps.js";
const h = {
  fontSize: !0,
  fontWeight: !0
}, o = r.span.withConfig({
  shouldForwardProp: (t, e) => !h[t] && e(t)
})`
  ${i}
  ${a}

  // These properties need to come after {variantStyle}, because they might
  // overwrite a variant attribute
  font-weight: ${({ theme: t, fontWeight: e }) => t.fontWeights[e]};
  font-size: ${({ theme: t, fontSize: e }) => t.fontSizes[e]};
  line-height: ${({ theme: t, lineHeight: e }) => t.lineHeights[e]};
  color: ${n};
  text-align: ${({ textAlign: t }) => t};
  text-transform: ${({ textTransform: t }) => t};
`;
o.defaultProps = p;
o.propTypes = s;
export {
  o as Typography
};
